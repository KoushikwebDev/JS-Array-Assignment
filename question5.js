// 5. Split the string into an array using split() method.

let newString = "hitesh sir is doing great work for us";

let intoArray = function (newString) {
  return newString.split(" ");
};
let newArr = intoArray(newString);
console.log(newArr);
